syntax = "proto3";
option go_package = "github.com/chainloop-dev/chainloop/app/controlplane/plugins";

service PluginService {
	rpc Hi (HiRequest) returns (HiResponse);
}

message HiRequest {
}

message HiResponse {
    string message = 1;
}